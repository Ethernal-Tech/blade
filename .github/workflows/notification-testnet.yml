---
name: Testnet Notification
on: # yamllint disable-line rule:truthy
  workflow_call:
    inputs:
      bat_network:
        type: string
        required: true
      bat_availability_test_status:
        type: string
        required: true
      bat_produce_blocks:
        type: string
        required: true
      bat_fund_status:
        type: string
        required: true
      bat_check_deployed_smart_contract_status:
        type: string
        required: true
      bat_method_set_status:
        type: string
        required: true
      bat_method_get_status:
        type: string
        required: true
      pp_network:
        type: string
        required: true
      pp_availability_test_status:
        type: string
        required: true
      pp_produce_blocks:
        type: string
        required: true
      pp_fund_status:
        type: string
        required: true
      pp_check_deployed_smart_contract_status:
        type: string
        required: true
      pp_method_set_status:
        type: string
        required: true
      pp_method_get_status:
        type: string
        required: true
    secrets:
      AWS_S3_BLADE_BUCKET:
        required: true
      SLACK_WEBHOOK_URL:
        required: true

jobs:
  notification:
    name: Notification
    runs-on: ubuntu-latest
    steps:
      - name: Short SHA
        id: short_sha
        run: echo "value=`echo ${{ github.sha }} | cut -c1-7`" >> $GITHUB_OUTPUT
      - name: Notify Slack
        uses: slackapi/slack-github-action@v1.25.0
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
          succeed_bnt: "primary"
          failed_bnt: "danger"
          succeed_job: ":green:"
          failed_job: ":red:"
        with:
          payload: |
            {
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "Nightly Testnet"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "Workflow Run"
                      },
                      "style": "${{ inputs.bat_produce_blocks == 'true' && inputs.bat_fund_status == 'true' && inputs.bat_check_deployed_smart_contract_status == 'true' && inputs.bat_method_set_status == 'true' && inputs.bat_method_get_status == 'true' && inputs.pp_produce_blocks == 'true' && inputs.pp_fund_status == 'true' && inputs.pp_check_deployed_smart_contract_status == 'true' && inputs.pp_method_set_status == 'true' && inputs.pp_method_get_status == 'true' && env.succeed_bnt || env.failed_bnt }}",
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                },
                {
                  "type": "context",
                  "elements": [
                    {
                      "type": "mrkdwn",
                      "text": "Network: *${{ inputs.bat_network }}*"
                    }
                  ]
                },
                {
                  "type": "divider"
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.bat_produce_blocks == 'true' && env.succeed_job || env.failed_job }} *Produce Blocks*"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.bat_fund_status == 'true' && env.succeed_job || env.failed_job }} *Fund Account*"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.bat_check_deployed_smart_contract_status == 'true' && env.succeed_job || env.failed_job }} *Deploy Smart Contract*"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.bat_method_set_status == 'true' && env.succeed_job || env.failed_job }} *Method set()*"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.bat_method_get_status == 'true' && env.succeed_job || env.failed_job }} *Method get()*"
                    }
                  ]
                },
                {
                  "type": "context",
                  "elements": [
                    {
                      "type": "mrkdwn",
                      "text": "Network: *${{ inputs.pp_network }}*"
                    }
                  ]
                },
                {
                  "type": "divider"
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.pp_produce_blocks == 'true' && env.succeed_job || env.failed_job }} *Produce Blocks*"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.pp_fund_status == 'true' && env.succeed_job || env.failed_job }} *Fund Account*"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.pp_check_deployed_smart_contract_status == 'true' && env.succeed_job || env.failed_job }} *Deploy Smart Contract*"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.pp_method_set_status == 'true' && env.succeed_job || env.failed_job }} *Method set()*"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "${{ inputs.pp_method_get_status == 'true' && env.succeed_job || env.failed_job }} *Method get()*"
                    }
                  ]
                }
              ]
            }
